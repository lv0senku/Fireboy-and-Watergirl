const fireBoy = new Person(0, 0, 20, 20);

let canvas = document.querySelectorAll("#display > canvas");
let ctx = canvas[1].getContext("2d");

setInterval(function() {}, 300);

window.onkeydown = function(event) {
  switch (event.key) {
    case "ArrowRight":
      break;
  }
};
